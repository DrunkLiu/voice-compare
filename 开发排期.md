# 纯语音对比功能 - 开发排期

## 基本信息

- 开始日期：2026-08-14
- 每日投入：2-3 小时（可自行调整）
- 预计周期：4-5 周
- 技术基础：Java/Spring + JavaScript 基础，Python 从 0 开始

## 里程碑总览

| 里程碑 | 时间节点 | 定义完成 |
|---|---|---|
| M0 环境就绪 | 已达成 | Python、FFmpeg、VSCode、.venv 可用 |
| M1 上传接口可跑 | 第 1 周（已提前完成） | 接口和上传页面已跑通；git 提交待做 |
| M2 能转写音频 | 第 2 周结束 | 输入音视频，返回英文转写文本 |
| M3 词级时间戳 | 第 3 周中 | 转写结果带单词时间点 |
| M4 三层对比 JSON | 第 4 周初 | 输出句子/单词/音素三层结果 |
| M5 前端分层展示 | 第 4 周结束 | 页面能完整展示对比结果 |
| M6 离线可用 | 第 5 周结束 | 无网环境能启动并完成对比 |

## 第 1 周：Python 基础 + FastAPI 后端

本周目标：不再害怕 Python，把“上传文件”这个最小链路跑通。

- [ ] Day 1：配置 VSCode 解释器为 `.venv`；学 Python 基础：变量、list、dict、函数
- [ ] Day 2：学文件读写、pathlib、JSON；写一个脚本统计文本文件单词数
- [x] Day 3：创建 `app/main.py`；写第一个 FastAPI `GET /health`
- [x] Day 4：实现 `POST /api/upload`，把上传的音频保存到 `uploads/`
- [x] Day 5：实现 `GET /api/files`，返回文件名、大小、类型
- [ ] Day 5b：初始化 git 仓库并提交一次

验收：`python -m uvicorn app.main:app --reload` 启动后，浏览器能上传文件并在页面看到文件信息。

## 第 2 周：音频处理 + faster-whisper

本周目标：让电脑第一次“听懂”英语音频。

- [ ] Day 6：学 FFmpeg 常用命令；用命令行把视频抽成 16kHz wav
- [ ] Day 7：用 Python `subprocess` 调 FFmpeg；用 soundfile 读取时长和采样率
- [ ] Day 8：在 `.venv` 安装 faster-whisper；跑通一段英文音频的转写
- [ ] Day 9：封装 `/api/transcribe`，输入文件路径，返回文本和分段
- [ ] Day 10：用一段真实英语视频做端到端测试，把结果保存为 JSON；git 提交

验收：输入任意音频或视频文件，接口能返回完整英文转写。

## 第 3 周：词级时间戳 + 音素对齐

本周目标：从“听清句子”升级到“知道每个词和音素在哪里”。

- [x] Day 11：开启 faster-whisper 词级时间戳（避免额外引入 WhisperX 重依赖）
- [ ] Day 12：安装 phonemizer + espeak-ng，把参考文本转成音素序列
- [ ] Day 13：尝试安装 Montreal Forced Aligner；装不动就记录问题并继续
- [ ] Day 14：完成参考音素与用户音频的词/音素对齐
- [ ] Day 15：把转写、时间戳、对齐结果封装成独立模块；git 提交

验收：能输出类似 `word -> start/end -> phones` 的结构化数据。

## 第 4 周：评分 + 前端分层展示

本周目标：做出第一个真正能“指出错误”的版本。

- [ ] Day 16：实现单词级差异检测：替换、漏读、多读
- [ ] Day 17：实现音素级 GOP 评分，低于阈值标记错误
- [ ] Day 18：汇总句子分，定义三层 JSON 结构
- [ ] Day 19：做前端页面：上传区、开始对比按钮、结果区
- [ ] Day 20：展示句子评分、问题单词标红、音素详情；git 提交

验收：上传参考音视频和用户录音，页面能分层显示哪句、哪个词、哪个音素有问题。

## 第 5 周：体验完善 + 离线打包

本周目标：从“能跑”变成“日常能用”。

- [ ] Day 21：加录音功能或本地音频播放，让回听更顺畅
- [ ] Day 22：用 pywebview 把网页包成本地桌面窗口
- [ ] Day 23：冻结 requirements，整理模型缓存目录和启动脚本
- [ ] Day 24：做离线分发目录或 exe 包，在无网环境测试
- [ ] Day 25：最终回归测试、补 README、git 提交

验收：断网状态下双击启动，能完成一次完整对比。

## 每日工作状态模板

每天结束时在排期文件里补上三行：

```text
今天目标：...
完成情况：...
卡点 / 下一步：...
```

## 立即行动：今天先做 30 分钟

1. 打开 VSCode，工作目录为本项目根目录
2. 新建 `app/main.py`，写一个返回 `{"status": "ok"}` 的 FastAPI 接口
3. 在终端运行 `python -m uvicorn app.main:app --reload`
4. 打开 `http://127.0.0.1:8000/docs`，看到自动生成的 API 文档

完成这一步，你就进入了工作状态。

## 规则

- 某一天卡住超过一天，不要停，先做下一个能推进的任务，回头再补
- 每完成一个里程碑就 git commit 一次，形成可见进度
- 每天只定一个最小目标，不要用“今天要把整个模块写完”这种大目标
