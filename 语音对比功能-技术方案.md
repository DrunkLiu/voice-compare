# 纯语音发音对比功能：技术方案

## 1. 需求概述

用户提供一段“参考”英语音视频，再录制一段自己的发音，软件在纯离线环境下完成比对，找出明显的发音错误，并按三个粒度分层展示：

- 句子层：整体评分、流利度
- 单词层：哪些词读错、漏读、吞音
- 音素层：具体哪个音标发音不准

## 2. 技术栈清单

| 层级 | 技术 | 用途 | 说明 |
|---|---|---|---|
| 开发语言 | Python 3.11 | 主语言 | 语音生态最完整 |
| 后端框架 | FastAPI + uvicorn | 本地 Web 服务 | 类似 Spring Boot 的轻量替代 |
| 前端 | HTML + CSS + JavaScript | 页面与交互 | 不用构建工具，纯静态资源 |
| 音频可视化 | wavesurfer.js | 波形展示与播放联动 | 本地文件，不依赖 CDN |
| 数据可视化 | ECharts | 句子/单词/音素评分图表 | 本地文件 |
| 桌面壳 | pywebview（可选） | 把网页包成本地桌面窗口 | 可后期再加 |
| 音视频处理 | FFmpeg + soundfile | 抽音频、统一采样率、读取音频 | 已在系统 PATH |
| 语音转写 | faster-whisper | 离线英文 ASR | 输出文本和置信度 |
| 词级时间戳 | WhisperX | 单词对齐到音频时间点 | 基于 faster-whisper |
| 音素序列 | phonemizer + espeak-ng | 把参考文本转成预期音素 | 生成“正确读法” |
| 词/音素对齐 | Montreal Forced Aligner | 把用户音频对齐到词和音素 | 输出 TextGrid |
| 发音评分 | SpeechBrain 或 Wav2Vec2/HuBERT + GOP | 计算每个音素发音置信度 | 识别明显错误 |
| 依赖管理 | pip + venv + requirements.txt | 环境隔离与离线安装 | 已配置 |
| 流程图 | draw.io | 流程设计 | 见 [语音对比功能-流程图.drawio](./语音对比功能-流程图.drawio) |
| 离线打包 | PyInstaller（后期） | 打包成 exe | 等核心链路稳定后再做 |

## 3. 实现原理

### 3.1 句子层

用 faster-whisper 分别转写参考音频和用户音频，得到两段文本。句子评分综合以下信号：

- 用户转写与参考文本的相似度（编辑距离 / WER）
- ASR 输出的置信度
- 语速、停顿等流利度指标

### 3.2 单词层

WhisperX 给用户语音的每个单词打上时间戳，再与参考文本做对齐，找出：

- 替换：读成了别的词
- 删除：漏读
- 插入：多读了词

前端按时间轴把问题单词标红。

### 3.3 音素层

用 phonemizer 把参考文本变成预期音素序列；用 MFA 把用户音频强制对齐到词和音素；最后计算 GOP（Goodness of Pronunciation）分数，也就是“这个音素听起来像正确音素”的概率。分数低于阈值的音素标记为发音错误。

## 4. 整体流程

```text
参考音视频 ─┐
           ├─> FFmpeg 抽取音频 ─> faster-whisper 转写 ─> 参考音素序列
用户音视频 ─┘                                        ─> WhisperX 词级时间戳
                                                              │
                                                              v
                                                      MFA 词/音素对齐
                                                              │
                                                              v
                                                      发音评分 GOP
                                                              │
                                                              v
                                                      三层结果 JSON
                                                              │
                                                              v
                                                      前端分层展示
```

完整流程图见 [语音对比功能-流程图.drawio](./语音对比功能-流程图.drawio)，可直接用 draw.io 或 VSCode 插件打开。

## 5. 最小版本实施顺序

1. FastAPI 接口：上传音频文件，保存并返回文件信息
2. 接入 faster-whisper：输出用户音频转写文本
3. 接入 WhisperX：输出词级时间戳
4. 接入 phonemizer + MFA：完成词/音素对齐
5. 计算 GOP 评分，输出三层 JSON
6. 前端页面：上传、对比、分层展示

## 6. 离线部署要点

- 所有前端 JS 库放在本地静态目录，不用 CDN
- Whisper、MFA 等模型首次联网下载后缓存到本地，之后完全离线
- Python 依赖提前导出为离线 wheel 包
- FFmpeg 已安装，打包时需随 exe 一起分发
